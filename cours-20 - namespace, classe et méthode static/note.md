

Expliquer classe et méthode public statique
Expliquer namespace et using
(même namespace, tout les classes sont accessible.  Autre namespace, doit l'importer avec Using)
Le namespace System, qui contient Console, Math, etc. est un exception, il est importer par implicitement.  On peut désactiver les implicits import dans Projet > Modfier le fichier projet et écrire disable dans ImplicitUsing.  (le montrer pour l'explication, mais les étudiants n'ont pas à le faire.)

Présenter visual studio dans les lab
Faire programme console sans l'ordre supérieur de fonction

Découper le sommatif 1 en classe avec eux.
Leur faire découper le sommatif 2 par eux même.